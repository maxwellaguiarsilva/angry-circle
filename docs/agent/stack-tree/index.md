# Stack-Tree — Root

Single source of truth for all pending work, debt, and session frames. See `loop.md` for the contract.

## Current path

- **Active node:** `threshold-test-reconciliation.md` (depth 4) under `request.md` → `destruction-physics.md` → `self-destruction-blocker.md`
- **Next session:** work **T4** in `threshold-test-reconciliation.md` — reconcile the strong-impact test (~787): raise projectile `vy` to ≥ 400 (use 450–500, below ~1200) so the destroy occurs, re-verify the score assertion (`SCORING.hit + SCORING.destroy` vs the launch-bonus on level clear). Then T5–T7 (in any order), T8–T10, T11. After that pop to `threshold-recovery-close.md` for T12 → T13 → T14 (close + commit). Then D3.4 remainder in `impact-damage-tests.md`, then `puff-effect.md` (D4.1–D4.3), then `destruction-balance-qa.md` (D5.1–D5.3 closes the stack). `loop-runner-concurrency.md` (INFRA.1/2) is independent — schedule when the loop has no other mandate. **Note:** stray uncommitted `loop.md` diff (disaster-recovery step-4 renumber) — decide commit-or-discard.

## Tree

- `request.md` — depth 1 — the enhanced user request (Angry Circle requirements); backlog B1–B9 all done
  - `delta-analysis.md` — depth 2 — **done**; resolved into B1–B5
  - `loop-runner-infra-debt.md` — depth 2 — **done**; infra committed
  - `loop-runner-concurrency.md` — depth 2 — **pending**; infra hazard (runner spawns concurrent sessions); stack INFRA.1–INFRA.2
  - `destruction-physics.md` — depth 2 — **pending**; stack D1–D5 (impact-based destruction + puff effect), new request 2026-08-09
    - `self-destruction-blocker.md` — depth 3 — **pending**; D3.5 (stacked-target self-destruction, real-level blocker); D3.5.1 decision **done** → D3.5.2 impl a–d done/committed. **Next task.**
      - `threshold-test-reconciliation.md` — depth 4 — **pending**; D3.5.2-e remainder: T1–T11 (test reconciliation + regression tests + verify). **Next node.**
      - `threshold-recovery-close.md` — depth 4 — **pending**; T12–T14 (refresh D3.1–D3.4 notes, close nodes, commit).
    - `impact-damage-tests.md` — depth 3 — **pending**; D3.1–D3.4 test fixes (D3.4 waits for D3.5.2)
    - `puff-effect.md` — depth 3 — **pending**; D4.1–D4.3 (puff data/validation, effect entity, audio+QA)
    - `destruction-balance-qa.md` — depth 3 — **pending**; D5.1–D5.3 (balance, QA gate, close stack)

## Backlog (root level)

- None at root level. Pending work lives in the `destruction-physics.md` subtree (D3.5 → D3.1–D3.4 → D4 → D5) and `loop-runner-concurrency.md` (INFRA.1 → INFRA.2).

## Operational note — git history resets

`git-reset-repository` has collapsed history to a single `initial commit`. When this happens, old references to prior commits in the tree are stale — clear them and continue work normally (do not chase commits that no longer exist).

## Log

- 2026-08-09 — Intake session. `prompt.md` ingested as `request.md`; enhanced and decomposed. Delta-analysis substack scheduled. Working tree clean, 250 tests passing.
- 2026-08-09 — Delta-analysis substack popped. All ACs satisfied by code (`file:line` in `delta-analysis.md`); 250 tests green. Resolved backlog B1–B5 written to `request.md`. No code edited, no commit (analysis-only session).
- 2026-08-09 — B1 done (README stale link → `docs/agent/stack-tree/request.md`). Committed.
- 2026-08-09 — B2 done (removed dead `press`/`release` fields in `src/game/game.js`; adapted the tap test to assert observable `drag`/`state`). 250 tests green. Committed.
- 2026-08-09 — B3 done (canonical `materials` list in `data/identity.json`; dropped duck-typing in `src/main.js` and the real-data test; regression test added). 251 tests green. Committed.
- 2026-08-09 — B4 done (projectile spawns at release/pouch position, not the launcher rest point; fixed visual teleport; fail-fast validation for the release position). 252 tests green. Committed.
- 2026-08-09 — B5 QA gate run (request.md). Checks 1–3 and 5 pass (252 tests, visual render OK, console 0/0, compliance clean). Check 4 exposed **B6**: level 1 is unwinnable — `src/main.js` injected `left-x:0`/`right-x:800` side walls with restitution, so targets could only leave play through the top (y<0), unreachable by slingshot arcs. Design intent (`tests/game.test.js` `makePhysics`) is ground-y only, no side walls. Interim verification confirmed the fix direction (removing the walls made knockdowns score and targets leave play), but no commit was made; session handed off cleanly per loop.md. Tree then held B5 done, B6/B7 pending.
- 2026-08-09 — B6 done (dropped side walls from `src/main.js` physics config, kept `ground-y`; `data/boot.json` untouched; `tests/main.test.js` regression added). 254 tests green, browser boot clean (console 0/0, scene renders). Committed.
- 2026-08-09 — B7 done (QA check 4 playthrough). Proved full win path in browser: level 1 cleared in 2/3 launches → level 2 auto-loaded; level 2 cleared in 2/5 launches → win overlay rendered; tap restarted to level 1. Wood block knocked off within budget, so optional `data/boot.json` tuning skipped. 254 tests green, console 0/0, compliance clean. No code/data changes. All backlog items B1–B7 done; tree empty.
- 2026-08-09 — **Disaster recovery.** `loop-violation.md` present: a fresh-delta-analysis session (empty backlog) overflowed the 100k budget mid-analysis and was killed by the loop runner before it could register findings. Its transcript shows it had verified two concrete gaps and was about to write them: (1) R1.1 DRY duplication of the circle-vs-rect closest-point math in `src/engine/physics.js` (`overlapping` vs `solveCircleRect`); (2) slingshot drag not anchored — `PointerInput.handlePointerDown` set `origin` to any pointer position, pouch teleported to the press point, `max-drag` capped power but not pouch position. Both re-verified against source in this session and registered as **B8** (DRY helper) and **B9** (drag anchoring, decision required) on `request.md`. `loop-violation.md` deleted. No commit (analysis/recovery session).
- 2026-08-09 — B8 done (DRY closest-point helper). Extracted `closestPointDeltaOnRect(circle, rect)` in `src/engine/physics.js` and used it in both `overlapping()` and `solveCircleRect()` (R1.1); the `dist === 0` branch now calls `halfWidth`/`halfHeight` directly. 3 regression tests added to `tests/physics.test.js` (edge clamp, inside-rect zero delta, corner clamp). 257 tests green, `bun build src/main.js --target browser` OK. Committed. Tree then held B8 done, B9 pending (decision required: slingshot drag anchoring).
- 2026-08-09 — **Disaster recovery.** A second `loop-violation.md` present: the B9 worker session (slingshot drag anchoring) overflowed the 100k budget mid-QA and was killed before it could commit or update the tree. Transcript verified against the working tree: B9 was **implemented and green on disk** — `Slingshot.isGrab`/`clampPouch` (fail-fast), `Game` grab-gate + clamped drag + anchored launch, 7 new slingshot tests + 5 new/2 adapted game tests, **268 tests green**, build OK, compliance audit passed, rest-render pixel ground truth OK, grab gate verified live; killed mid-playthrough during the drag-clamp pointer dispatch. The tree's B9 node was rewritten to reflect this state and the exact remaining steps (finish QA playthrough, commit the 4 B9 files, mark done). **New debt registered:** uncommitted loop-runner infra edits in `loop.md`, `run-loop.sh`, `docs/agent/compliance-rules.md` (watchdog/INTAKE/BUDGET/VIOLATION docs + rules R4.4/R5.2 removal) — separate from B9, needed a commit-or-discard decision. `loop-violation.md` deleted. No commit (recovery session; the working tree kept the B9 work exactly as the overflowed session left it).
- 2026-08-09 — **B9 done + infra debt done.** Resumed B9 per the tree: finished the browser QA playthrough smoke check (drag-clamp case). Live verification: far press (700,500) → no grab, pouch stays seated, release launches nothing (pixel + visual ground truth); drag beyond `max-drag` (launcher 120,520 → pointer 0,400, dist 169.7) → pouch clamps to the 150px radius at (13.9,413.9); anchored release → projectile spawns at the clamped pouch and flies opposite the launcher vector (pixel centroid tracked in flight: x 101→271 up-right). 268 tests green, `bun build` OK, console 0 errors (1 warning was the probe's own `getImageData` readback, not a game defect). Committed the 4 B9 files and marked B9 done in `request.md`. Loop-runner infra debt decided **COMMIT**: `run-loop.sh` watchdog verified operational (probe symlink + jq + `opencode export` all present; the two prior `loop-violation.md` events prove the export path). QA screenshot `b9-render-rest.png` removed (untracked QA artifact). Working tree clean. Tree: all backlog done; no active mandate.
- 2026-08-09 — **Intake.** New `prompt.md` ingested: (1) operational note — git history may be reset to a single initial commit by `git-reset-repository`; on that event, clear stale commit references in the tree and continue normally (stale hashes cleaned from this log); (2) feature — object destruction physics: targets currently die only by being pushed off screen; add impact-based destruction where targets "go puff" like the cloned game, keeping the IP-safe identity (generic colored circles, generic puff, no characters). Internalized as substack `destruction-physics.md` (depth 2) with stack D1–D5; D1 (destruction-model decision) is the next task. `prompt.md` deleted. No commit (intake-only).
- 2026-08-09 — **D1 done** (destruction model decided, analysis-only). Decision recorded in `destruction-physics.md` `Decisions`: damage metric = relative normal closing speed per contact (game-agnostic `impact` px/s, captured on first solver iteration); blocks destructible too (all targets carry `hp`); off-screen elimination kept as fallback kill (mutually exclusive with destroy, no double-score); new `destroy` scoring entry; puff = generic fading ellipse burst + `puff` tone (data-driven, IP-safe); engine contract for D2 (`onCollision({a,b,impact})`, shared-helper, no DRY duplication); data layout for D3/D4; AC mapping; post-flow mapping note. No code, no commit (analysis-only).
- 2026-08-09 — **D2 done** (engine impact data). `Physics` collision callback now carries `impact` (px/s): the relative normal closing speed, captured once per pair per step on the first solver iteration before resolution, using a shared `relativeNormalSpeed` helper (R1.1 DRY — `applyImpulse` reuses it). `applyImpulse` returns the magnitude; the solve functions propagate it; `solveCollisions` records it into a per-step `impacts` map keyed like `contacts`; `emitContacts` passes `{a,b,impact}`. Engine stays game-agnostic (R1.2/R2.1 — no game concepts). 5 regression tests in `tests/physics.test.js` (head-on closing speed = 10; pre-resolution capture distinct from post-resolve separation; separating = 0; resting = 0; circle-rect = 50). 273 tests green, `bun build` OK, compliance verifier clean. Committed (physics.js + physics.test.js + tree).
- 2026-08-09 — **Disaster recovery.** `loop-violation.md` present: the D3 worker session (HP/damage model) overflowed the 100k budget mid-QA and was killed before it could update the tree. Transcript verified against the working tree: D3 was **fully implemented on disk and committed** (history collapsed into `initial commit`) — `identity.json` `hp`, `boot.json` `scoring.destroy`, `identity.js`/`game.js` hp validation + `applyImpactDamage` + `destroyTarget`, plus new D3 tests. But **4 tests fail** (`bun test`: 281 pass, 4 fail), and the session was killed mid-debug of them (its last tool call was a debug script on the repeated-impacts + settle tests). The D3 node in `destruction-physics.md` was rewritten with the exact 4 failures and their analysis (D3.1 scoring-validation test mismatch, D3.2 launch-bonus inflates destroy-score assertion, D3.3 second-impact closing speed neutralized by target's own fall, D3.4 settle-counter off-by-one). `loop-violation.md` deleted. No commit (recovery session).
- 2026-08-09 — **Disaster recovery #2 — D3.4 corrected + D3.5 blocker found.** A fresh `loop-violation.md` (re-exported transcript) triggered a second recovery pass. Independent re-verification against source corrected the prior D3.4 diagnosis: it is **not** a settle off-by-one. Root cause: `Game.applyImpactDamage` damages **both** bodies of every contact including target-target pairs, so the two stacked circles in the `LEVEL` fixture (36px apart = exactly touching) collide under gravity (~326 px/s) and destroy each other → level clears → state `won` → `markSettledProjectiles` stops → `settleFrames` freezes at 11, `spent` undefined. **Same bug breaks the real game**: `data/levels.json` level 1 stacks circles at y=560/524/488 + wood/stone blocks; a real `Game` boot auto-destructs 4/5 targets within 60 frames (score 100, no input). Level 2 has the same stacked-circle pattern. Registered **D3.5** (blocker) in `destruction-physics.md` with fix directions (min-impact threshold / higher hp / non-touching layouts). D3.1–D3.3 notes confirmed correct. `loop-violation.md` deleted (git rm; it had been snapshot into `initial commit`). No commit (recovery session). Note: the loop runner is spawning concurrent opencode sessions; `probe.mjs`/`probe2.mjs` are an active worker's untracked QA artifacts.
- 2026-08-09 — **Disaster recovery #3 — plan registered (`hidden-comet` overflow).** `loop-violation.md` present: the `hidden-comet` session (a `loop.md` execution review that itself entered disaster recovery for a `proud-pixel` transcript) overflowed the budget. Its processed plan confirmed the prior recovery's findings were already in the tree (D3.4 root cause = stacked-target self-destruction; D3.5 real-level blocker; D3.1–D3.3 confirmed) and required no rework. Registered the plan's decomposition into the tree: pushed **`self-destruction-blocker.md`** (D3.5.1 decision → D3.5.2 impl+regression; the next task), **`impact-damage-tests.md`** (D3.1–D3.4, D3.4 after D3.5.2), **`puff-effect.md`** (D4.1–D4.3), **`destruction-balance-qa.md`** (D5.1–D5.3 closes the stack), and **`loop-runner-concurrency.md`** (INFRA.1–INFRA.2, independent infra hazard: runner spawns concurrent sessions PIDs 43769/44811 racing tree edits). Repointed `destruction-physics.md` D3–D5 items to the children (node stays under 200 lines); `index.md` current path updated to `self-destruction-blocker.md`. `loop-violation.md` deleted. No commit (recovery session).
- 2026-08-09 — **D3.5.1 done** (decision, analysis-only). Decided the stacked-target self-destruction fix in `self-destruction-blocker.md`: **min-impact-damage threshold, data-driven (`destruction.min-impact-damage`, default 400 px/s), gated in `Game.applyImpactDamage`**. Grounded in probes against the real physics config: settle contacts peak at a stable **326 px/s** (stacks of 2/3/5/8 identical; real level 1 → 1173 settle contacts, max 326, block-block 16–20), while real projectile impacts are **941–1200 px/s** → 400 sits cleanly between. Rejected: raising `hp` (settle bounces accumulate, 326+261+231+… + thousands of small bounces), projectile-only damage (kills D1 chain reactions), and reworking level layouts (band-aid; D3.4 fixture intentionally stacks touching circles). The 400 threshold unblocks both the real levels (settle 326 < 400) and the D3.4 fixture without touching it. No code, no commit (analysis-only). D3.5.2 implementation scheduled next.
- 2026-08-09 — **Contract-recovery intake (harness-TODO violation).** The D3.5.2 worker used the opencode TODO tool instead of the stack-tree; the loop runner killed it and queued this intake. Registered every captured item as per-session tasks in `self-destruction-blocker.md`: D3.5.2 decomposed into D3.5.2-a (read scoped docs) → D3.5.2-b (boot.json threshold) → D3.5.2-c (game.js gate) → D3.5.2-d (main.js wiring) → D3.5.2-e (regression tests) → D3.5.2-f (verify: bun test/compliance/build) → D3.5.2-g (mark done) → D3.5.2-h (commit), priorities preserved. No work executed, no code touched. `prompt.md` deleted. No commit (intake-only).
- 2026-08-09 — **Disaster recovery (D3.5.2 overflow).** `loop-violation.md` present: the D3.5.2 worker session overflowed mid-test-reconciliation. Its processed plan confirmed D3.5.2-a…d are on disk and committed (`data/boot.json:38-39`, `src/game/game.js` `destruction` dep + gate, `src/main.js` wiring); suite at 97 pass / 6 fail (all threshold/fixture casualties: fixture gaps ~763/~771, sub-threshold speeds ~787/~811/~833/~860). Registered the plan: pushed `threshold-test-reconciliation.md` (T1–T11: re-baseline, fixture audit/fixes, reconcile the four D3 impact tests, three regression tests, verify) and `threshold-recovery-close.md` (T12–T14: refresh stale D3.1–D3.4 notes, close nodes, commit) as depth-4 children of `self-destruction-blocker.md`; recorded the scope decision (D3 impact-test reconciliation absorbed into D3.5.2's scope, D3.4 resolved by the gate). Updated current path → `threshold-test-reconciliation.md`. `loop-violation.md` deleted. No commit (recovery session).
- 2026-08-09 — **T1 done** in `threshold-test-reconciliation.md`. Re-baselined `bun test tests/game.test.js`: 97 pass / 6 fail, exactly the 6 known casualties (constructor-throw fixtures at :763/:771, strong-impact :787, weak-impact :811, repeated-impacts :833, target-impacted-by-target :860), no new failures. Evidence recorded in the node. Tree updated; T2 (fixture wiring audit) next. No commit (explore-only task).
- 2026-08-09 — **T2 done** in `threshold-test-reconciliation.md`. Audited all 23 constructor-throw `base` fixtures + `makeGame` + real-boot construction in `tests/game.test.js`. Found **4** bases lacking `destruction` (not 2): known failures at 764/772 (test lines 763/771) plus silent stragglers at 759 and 1414 (multiline material-accessor base), which pass only because the `scoring`/`identity.material` checks in `game.js` fire before the `destruction` check. T3 expanded to fix all 4. No commit (explore-only task).
- 2026-08-09 — **T3 done** in `threshold-test-reconciliation.md`. Added `destruction: DESTRUCTION` to all 4 straggler bases (759, 764, 772, 1414); validation order preserved (scoring/material checks still fire before destruction). One assertion fix: the invalid-scoring-block override `{ hit: 10, knockdown: 25 }` now throws `"destroy"` first (validateScoring loops `["hit","knockdown","destroy","launch-bonus"]`, game.js:211) — added `destroy: 50` so the `"launch-bonus"` assertion still fires. Constructor-throw group 23/23 green; full game suite 99 pass / 4 fail (763/771 casualties fixed; remaining 4 are the T4–T7 targets). Committed (311d3e2). Stray uncommitted `loop.md` diff noted for a commit-or-discard decision.
